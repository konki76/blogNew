{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_grp_edit' %}

{% block main %}
    <h1>Edit skill #{{ skill.id }}.</h1>

    {{ include('admin/blog/_form.html.twig', {
        form: edit_form,
        button_label: 'Save changes',
    }, with_context = false) }}
{% endblock %}

{% block sidebar %}
    <div class="section actions">
        {{ include('admin/blog/_form.html.twig', {
            form: delete_form,
            button_label: 'Delete skill',
            button_css: 'btn btn-lg btn-block btn-danger',
            include_back_to_home_link: false
        }, with_context = false) }}
    </div>

    {{ parent() }}


{% endblock %}
