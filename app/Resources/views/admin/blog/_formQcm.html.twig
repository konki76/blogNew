{{ form_start(formQcm) }}
    {{ form_widget(formQcm) }}

    <input type="submit" value="{{ button_label|default("Create Qcm") }}"
           class="{{ button_css|default("btn btn-primary") }}" />

    {% if include_back_to_home_link is not defined or include_back_to_home_link == true %}
        <a href="{{ path('admin_qcm_index') }}" class="btn btn-link">
            Back to the Qcm list
        </a>
    {% endif %}
{{ form_end(formQcm) }}
