{% extends 'base.html.twig' %}

{% block body_id 'blog_index' %}

{% block main %}

    <h2>{{ ue.title }}</h2>

	Liste des questionnaires de cette UE : <br/>
	
	{% for grp in grps %}
	- <a href="{{ path('blog_grpPage', { slug: grp.slug }) }}" >{{ grp.title }}</a><br>
	{% endfor %}

{% endblock %}

{% block sidebar %}
    {{ parent() }}
{% endblock %}
